apiVersion: v1
kind: Service
metadata:
  name: haproxy
  namespace: haproxy
spec:
  type: ClusterIP
  selector:
    app: haproxy
  externalIPs:
    - 10.0.0.7
  ports:
    - { name: http, port: 90, targetPort: 8080 }
  externalTrafficPolicy: Cluster
  internalTrafficPolicy: Cluster
