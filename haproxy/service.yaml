apiVersion: v1
kind: Service
metadata:
  name: haproxy
  namespace: haproxy
spec:
  type: ClusterIP
  selector:
    app: haproxy
  externalIPs:
    - 10.0.0.4
  ports:
    - { name: http, port: 80, targetPort: 8080 }
